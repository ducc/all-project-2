// FRONTEND BUILD CONFIG FILE
// MAKE A COPY OF THIS FILE AND RENAME IT TO config.js

// only edit fields inside of this Config object
const Config = {
    // set this to the url the subscriber's api is running on
    // example: "http://localhost:1337/"
    API_URL: "http://localhost:1337/"
}

// touching this will probably break it
export default Config